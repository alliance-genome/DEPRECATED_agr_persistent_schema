id: https://github.com/alliance-genome/agr_persistent_schema/src/schema/publisher_resource.yaml

imports:
  - cross_reference
  - author_reference

classes:
  publisher resource:
    slots:
      - first name
      - middle names
      - last name
      - initials
      - has cross references