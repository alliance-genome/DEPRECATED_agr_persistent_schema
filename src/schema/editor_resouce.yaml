id: https://github.com/alliance-genome/agr_persistent_schema/src/schema/editor_resource.yaml

imports:
  - cross_reference
  - author_reference

classes:
  editor resource:
    is_a: information content entity
    slots:
      - first name
      - middle names
      - last name
      - initials
      - has cross references
