id: https://github.com/alliance-genome/agr_persistent_schema/src/schema/editorResource

imports:
  - core
  - authorReference

classes:
  editor resource:
    is_a: information content entity
    slots:
      - first name
      - middle names
      - last name
      - initials
      - cross references
